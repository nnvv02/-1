# Завдання 3: Пошук слова, що починається на "к"
sentence = input("Введіть речення: ")

# Розділяємо речення на слова
words = sentence.split()

# Шукаємо перше слово, що починається на літеру "к"
found = False
for word in words:
    if word.lower().startswith('к'):  # Перевіряємо на початкову літеру "к" (враховуючи регістр)
        print("Знайдене слово, що починається на 'к':", word)
        found = True
        break

if not found:
    print("У реченні немає слів, що починаються на літеру 'к'.")
